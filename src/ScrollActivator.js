import React, { Component } from 'react'
import PropTypes from 'prop-types'

class ScrollActivator extends Component {
  static propTypes = {
    containerElement: string.PropTypes.isRequired
  }

  componentDidMount () {}
}

export default ScrollActivator
